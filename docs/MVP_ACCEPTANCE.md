# MVP 验收清单

> 本文是交付门禁，未勾选项不代表已完成。当前预置任务 `XC2026` 打通了 H5 资料、Mock 访谈和提交；管理端新建任务只保存到 `localStorage`，访谈/融合/Reference 视图使用预置数据，因此还不是共享实时状态的端到端闭环。应用默认使用 Mock；显式设置 `EXPERIENCE_DATA_PROVIDER=supabase` 且提供服务端凭证后，API/Service 运行时会读写 Supabase。

## 自动检查

- [x] `npm run lint` 无 error / warning。
- [x] `npm run typecheck` 通过。
- [x] `npm run test` 覆盖邀请码、五维追问推进、融合输出与 Reference 结构。
- [x] `npm run build` 成功生成生产构建。
- [ ] `supabase db reset` 可在干净本地实例上重放 migration 与 seed，seed 只含合成数据。

> 2026-08-21 交付审计：前四项已在当前工作区执行通过。当前环境未安装 Supabase CLI 与 `psql`，因此数据库重置项仅完成静态检查，未标记为通过。

## PC 管理端

- [x] Dashboard 能进入任务、创建任务、访谈与机构经验页面。
- [ ] 创建向导可配置基本信息、参与人字段、Agent / 案例规则并发布。
- [x] 发布页提供邀请码、可复制链接与等价二维码。
- [x] 访谈详情同时展示原始对话、五维覆盖、个人萃取案例和经验规则。
- [x] API 只允许同任务、已提交且已完成萃取的访谈参与融合。
- [x] Reference 可预览、复制 Markdown 并下载 `.md`。

> 管理端页面回归已通过；创建向导尚未接入 Mock API，因此保留为未完成项，而不是把 `localStorage` 发布误报为端到端发布。

## H5 业务员端

- [x] 正确邀请码 `XC2026` 可进入；无效邀请码有明确错误。
- [x] 用户必须同意数据用途说明并填写动态必填字段。
- [x] 案例挑战将事实、推测和挑战问题分开展示。
- [x] 文本回答后，Mock Agent 根据第一个未完整维度继续追问。
- [x] 模拟录音可开始、停止、产生明确标识的模拟转写，并允许编辑。
- [x] 自动保存状态通过 `aria-live` 通知，刷新后能恢复访谈。
- [x] 提交完成页显示 Mock API 个人案例和“待后台审核”边界。

## 视觉与可访问性

- [ ] 320、390、768、1280、1440px 视口无横向溢出。
- [x] H5 输入区使用 `100dvh` / safe area，不被移动浏览器栏遮挡。
- [x] 触控目标不小于 44×44px。
- [x] 表单有真实 label；错误与说明文本通过 `aria-describedby` 关联。
- [x] 状态不仅用颜色表达；键盘可完成主链路。
- [x] `prefers-reduced-motion` 下停用非必要动画。
- [x] 浏览器控制台无 error / warning。

> 已对手机与桌面主视口做浏览器回归；完整的五档截图基线尚未固化为自动视觉测试，因此第一项仍保留为人工验收门禁。

## 能力边界

- [x] UI 明确标注演示数据、Mock Agent 与模拟转写。
- [x] 不把模拟挑战称为真实案例，不把融合草稿称为已验证机构经验。
- [ ] 未配置生产认证、租户隔离与隐私治理时，不允许输入真实敏感数据。
